/**
 * Timing constants for animations and delays
 */

/** Blinking cursor interval in milliseconds */
export const CURSOR_BLINK_INTERVAL_MS = 530;

/** Delay between boot sequence steps in milliseconds */
export const BOOT_STEP_DELAY_MS = 400;

/** Unlock animation duration in milliseconds */
export const UNLOCK_ANIMATION_DELAY_MS = 1500;

/** Total boot sequence steps */
export const BOOT_SEQUENCE_TOTAL_STEPS = 5;
