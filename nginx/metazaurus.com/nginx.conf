# Redirect www to non-www (301 permanent)
if ($host = www.metazaurus.com) {
    return 301 https://metazaurus.com$request_uri;
}

# SPA catch-all — route through PHP for bot detection / OG tags
location / {
    try_files $uri /index.php;
}
